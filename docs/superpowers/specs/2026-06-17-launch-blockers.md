# YEZZ 上线前阻断问题清单（Launch Blockers）

> **原则：不修复这些问题，上线不如不上线。**
> 以下 Issue 全部解决前，产品不应面向真实用户发布。
> **最后更新：** 2026-06-17

---

## 验收标准

上线前必须通过以下全部检查项：

- [x] 所有占位图已替换为真实内容图片
- [x] 联系页无原始 i18n key 暴露
- [x] API fallback 数据已移除或替换为真实数据
- [x] 中文页面标签/时长显示为中文
- [x] 购物车空字段有明确的错误提示
- [x] Hero CTA 跳转目标正确
- [x] Hero 按钮文字与实际跳转一致
- [x] 派对页有明确的预约/咨询出口
- [x] 购物车 Session 绑定请求来源，无法跨用户操作
- [x] Swagger UI 在生产环境不可访问
- [x] 文件上传增加 MIME magic bytes 校验
- [x] API 不返回任何密码字段（明文或哈希）
- [ ] 生产环境图片正常加载（需确认是配置还是代码问题）
- [x] Redis 宕机时服务仍有限流保护
- [x] 取消预约后对应时段容量正确回滚
- [x] 预约提交时校验时段 ID 和剩余容量

---

## 剩余待确认问题（1 项）

### 🏗️ 基础设施

#### BUG-002：生产图片全部无法加载

| 字段 | 内容 |
|------|------|
| **类别** | 基础设施 |
| **严重程度** | 阻断上线 |
| **位置** | 待确认（可能是 `fly.toml` / 环境变量 / 存储配置） |
| **问题** | 所有上传的图片在生产环境显示为破图 |
| **为什么必须修** | 网站等于没有图片，对于依赖视觉展示的项目型业务是致命问题。 |
| **修复方向** | **先诊断：** 检查生产环境的 `S3_PUBLIC_URL` / `NEXT_PUBLIC_MEDIA_URL` / CDN 域名配置是否正确。<br>代码层面 `next.config.ts` 已支持动态解析图片域名，请确认部署时环境变量已设置。 |
| **预估工作量** | 待诊断（配置问题 10 分钟，代码问题 2-4 小时） |

---

## 已修复问题（已归档）

以下问题已在代码中修复并通过类型检查/测试，详见 git history：

**安全与合规（5 项）：** SEC-005 Swagger 开关、SEC-001 登录限流（代码已有）、SEC-006 Cart Session IP 绑定、SEC-003 密码不返回、SEC-004 上传扩展名白名单

**基础设施（1 项）：** SEC-002 Redis 降级限流（代码已有 inMemoryRateLimit）

**转化路径（3 项）：** BUG-003 Hero CTA 改为 /book、CON-002 按钮与链接一致、BUG-008 派对页底部 CTA

**品牌信任（5 项）：** CON-001 移除占位图、BUG-001 i18n key 翻译完整、BUG-005 API 故障返回空状态、CON-004 mock 数据标签/时长中文化、CON-003 购物车空字段提示

**核心业务逻辑（2 项）：** BIZ-001 取消预约回滚容量（代码已有）、BIZ-002 时段容量校验（代码已有）

---

## 明确不在本清单中的事项（这些不是上线 blocker）

以下事项属于**优化、工程体验、运营效率**范畴，可以上线后分批处理：

- ❌ 前端数据层迁移到 TanStack Query + Zustand（当前 useEffect 能工作，只是样板代码多）
- ❌ Admin 列表分页（数据量小的时候不卡）
- ❌ 路由请求体验证 schema（当前 service 层有手工校验，功能正常）
- ❌ 错误监控 / 结构化日志 / 请求追踪 ID（运营侧问题，用户无感知）
- ❌ 健康检查检查下游依赖（运维监控问题）
- ❌ 邮件品牌包装 / 404 页面 / SEO 优化（体验杀手，但不是阻断）
- ❌ 数据库索引 / Session 清理 / 状态机约束（运营拖累）
- ❌ 购物车可删除 / 加购反馈 toast（体验优化）

---

> **下一步：** 确认 BUG-002（生产图片域名配置）后，本清单所有 blocker 清除，可以上线。
